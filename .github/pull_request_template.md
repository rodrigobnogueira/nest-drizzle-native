## Summary

- 

## Scope Check

- [ ] This PR is package/library work.
- [ ] This PR is sample/docs/CI work.
- [ ] If this is sample work, it does not include `packages/drizzle/**`
      changes.

For sample-only PRs, run:

```bash
git diff --name-only main...HEAD -- packages/drizzle
git diff --cached --name-only -- packages/drizzle
```

Both commands should print nothing.

## Validation

- [ ] Relevant local checks are listed below.

```bash
# command output summary
```

## Security Pass

- Dependency changes reviewed:
- Runtime behavior reviewed:
- Secrets or sensitive data checked:
